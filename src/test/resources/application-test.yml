spring:
  kafka:
    consumer:
      auto-offset-reset: earliest

oven:
  kafka:
    consumer:
      retry:
        interval: 10ms
        max-retries: 3
    topics:
      auto-create: true
      fulfillment: fulfillment-events
    consumer-groups:
      orders: oven-platform-orders-test
